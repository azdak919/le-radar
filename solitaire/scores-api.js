/* Public Cloudflare Worker endpoint for the shared Solitaire leaderboard.
 * Leave empty until the first deployment; local and static fallback scores
 * still work offline. This URL is public by design and contains no secret. */
window.LE_RADAR_HISCORES_API = '';
